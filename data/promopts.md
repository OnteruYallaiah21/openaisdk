1.
I am preparing notes in a Markdown (.md) file.

Requirements:

Organize content topic-wise.
Use small bullet points for each observation or note.
For each point, include a brief explanation or trade-off if applicable.
Example: “InputGuardrails runs in parallel by default. If we want sequential execution, we must explicitly set run_in_parallel=False. Otherwise, CPU usage increases unnecessarily.”
Capture all observations, important behaviors, and default settings.
Highlight key points to remember as concise bullets.
Keep the language clear and precise, suitable for reference.

Goal: A structured, topic-wise Markdown note file with numbered small observations, explanations, and trade-offs where relevant.